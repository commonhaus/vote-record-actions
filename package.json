{
  "name": "vote-record-actions",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "fix": "npx @biomejs/biome check --write ./src",
    "format": "npx @biomejs/biome format ./src",
    "lint": "npx @biomejs/biome lint ./src",
    "prebuild": "npx @biomejs/biome check ./src",
    "build": "node esbuild.config.mjs"
  },
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^20.12.12",
    "esbuild": "^0.25.0",
    "typescript": "^5.4.5",
    "yaml": "^2.3.4"
  },
  "dependencies": {
    "eta": "^3.5.0"
  }
}
